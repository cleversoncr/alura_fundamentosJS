const notas = [10, 6, 8]; // Com o método push, o elemento sempre será inserido no final

notas.push(7); // Método de arreay (função que recebe um parâmetro)

console.log(notas);

let media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length;

console.log(media);
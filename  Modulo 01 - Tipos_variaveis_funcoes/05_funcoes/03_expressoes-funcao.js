//deeclaração de função

// function minhaFuncao() {
//bloco de código

// };

// minhaFuncao('param');

//expressão de função

// const soma = function (x, y) { return x + y };

// console.log(soma(1, 1));

//diferença principal ????

// console.log(apresentar());

// function apresentar() {
//   return 'Ola';

// };

/*
  Uma expression funcion se comporta como se fosse uma variável, ou seja,
  é necessário "declarar" a variável antes de tentar utiliza-la
  A exceção é se ela fosse declarada como uma VAR.
 */

console.log(soma(1, 1));

const soma = function (x, y) { return x + y };

/*
  Antes de executar qualquer linha no código, o JS lista todas as VAR e funções.
  Esse processo se chama HOISTING.
*/ 